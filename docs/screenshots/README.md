# Screenshots

Drop four PNGs here to populate the README:

- `benches.png` — Benches tab with the cards list + stats banner
- `bench-detail.png` — bench detail with live log tail + action buttons
- `install.png` — installer view mid-run
- `sites.png` — cross-bench Sites table

Tip: use Flameshot or GNOME Screenshot (`gnome-screenshot -w` for
window-only). Aim for ~1280×800 for a clean README render.
